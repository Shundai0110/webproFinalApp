generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

// Domain models are intentionally not defined yet.
// Add models only when the corresponding feature is implemented.
